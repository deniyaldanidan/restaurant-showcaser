import { Cinzel, Cinzel_Decorative, Montserrat } from "next/font/google";

export const cinzel = Cinzel({
  subsets: ["latin"],
  display: "swap",
});

export const cinzel_decorative_reg = Cinzel_Decorative({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const cinzel_decorative_bold = Cinzel_Decorative({
  weight: "700",
  subsets: ["latin"],
  display: "swap",
});

export const montserrat = Montserrat({
  subsets: ["latin"],
  display: "swap",
});
